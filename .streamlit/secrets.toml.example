# For Streamlit Community Cloud: paste these into the app's "Secrets" settings
# (Manage app -> Settings -> Secrets). Do NOT commit the real secrets.toml.

OPENAI_API_KEY = "sk-your-key-here"
OPENAI_MODEL = "gpt-4o-mini"
